# Getestete Basis fuer die Jupyter-Notebooks in `notebooks/`
# Installation (im Projektverzeichnis):
#   pip install -r requirements-notebooks.txt
#
# Hinweis zu `qdrant_demo.ipynb`:
# - Das Notebook setzt bereits `TRANSFORMERS_NO_TF=1` / `USE_TF=0`.
# - Falls dennoch ein Keras-3-Fehler auftritt, zusaetzlich installieren:
#   pip install tf-keras

jupyter==1.1.1
numpy==1.26.4
pandas==2.2.2
matplotlib==3.10.8
scipy==1.14.1
sympy==1.14.0
torch==2.9.1
transformers==4.57.3
qdrant-client==1.14.1
sentence-transformers==3.0.1
